{
  "name": "hall-booking-calendar",
  "version": "1.3.0",
  "description": "A WordPress plugin to manage a hall calendar with 3 rooms, recurring bookings, and calendar subscriptions.",
  "repository": {
    "type": "git",
    "url": "https://github.com/mike-richardson-uk/hall-booking-calendar.git"
  },
  "keywords": [
    "wordpress",
    "plugin",
    "booking",
    "calendar",
    "hall-booking"
  ],
  "author": "Mike Richardson <iam@mike-richardson.com>",
  "license": "GPL-2.0+",
  "devDependencies": {
    "@wordpress/eslint-plugin": "^19.0.0",
    "eslint": "^8.57.0",
    "stylelint": "^16.2.0",
    "stylelint-config-standard": "^36.0.0",
    "stylelint-config-wordpress": "^18.0.0"
  },
  "scripts": {
    "lint": "npm run lint:js && npm run lint:css && npm run lint:php",
    "lint:js": "eslint assets/js/**/*.js",
    "lint:css": "stylelint assets/css/**/*.css",
    "lint:php": "composer run-script lint",
    "lint:fix": "npm run lint:js -- --fix && npm run lint:css -- --fix",
    "test": "echo \"No tests configured yet\" && exit 0"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  }
}
